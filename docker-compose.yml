services:
  h.server:
    # image: ${DOCKER_REGISTRY-}hserver
    image: tda:latest # Tour de app image tag
    container_name: h.server
    build:
      context: .
      dockerfile: src/h.Server/Dockerfile
  redis:
    image: redis
    container_name: redis
    ports:
      - "6379:6379"